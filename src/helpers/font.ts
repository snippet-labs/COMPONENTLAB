// Modules
import localFont from 'next/font/local';

const fontMonoton = localFont({
  src: '../../public/fonts/Monoton.ttf',
  display: 'swap',
  variable: '--font-monoton',
});

export default fontMonoton;
