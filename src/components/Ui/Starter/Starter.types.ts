// Global types
import { AccessibleComponentsVariantCardTypes } from '@/types';

export type StarterProps = {
  starterTitle: string;
  starterDescription: string;
  starterVariantDescription: string;
  starterVariantCards: AccessibleComponentsVariantCardTypes[];
};
