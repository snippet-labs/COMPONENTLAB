export const INSTALLATION_CODES_REACT_ICONS = `bun install react-icons`;
export const INSTALLATION_CODES_TAILWINDCSS = `bun install tailwindcss @tailwindcss/vite`;
export const INSTALLATION_CODES_VITE_CONFIG_PLUGIN = `import { defineConfig } from 'vite'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [
    tailwindcss(),
  ],
})`;
export const INSTALLATION_CODES_CSS_IMPORT = `@import "tailwindcss";`;
